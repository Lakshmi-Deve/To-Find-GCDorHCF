# To-Find-GCDorHCF
To find the GCD (Greatest Common Divisor) or HCF (Highest Common Factor) of two numbers, use the Euclidean algorithm. It repeatedly subtracts the smaller number from the larger one until both numbers are equal, which is the GCD.
